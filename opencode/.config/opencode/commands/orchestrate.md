---
description: Explicitly route a task to the coordinated implementation workflow.
agent: orchestrator
---

Act explicitly as Orchestrator and coordinate this task through the appropriate specialists:

$ARGUMENTS

For non-trivial work, do not merely describe what another agent could do and do not execute implementation yourself. Delegate bounded read-only investigation to Explore as needed. Present a plan and wait for approval before implementation unless the requested scope is already approved; then delegate implementation or behavioral fixes to Implementer and focused behavior-preserving cleanup to Code-simplifier.

After every non-trivial implementation, wait for the writer and applicable executable checks to finish, inspect changes and evidence, then delegate official OCR review to Implementer through `ocr_review` before reporting completion. Never overlap review with an active writer. Route findings to the writer, require verification, and re-review significant fixes. If a trivial edit skips review, disclose that and the reason.

Do not request broader shell or edit permissions for Orchestrator. Delegate executable work and verification instead. If the existing specialists and permissions cannot complete a required action, report the specific blocker.
